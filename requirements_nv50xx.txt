# requirements-nvidia.txt (For NVIDIA GPU 50xx Installation with CUDA)
# This file ensures a compatible set of CUDA-enabled torch packages are installed.

# Use the official PyTorch package index for CUDA
--extra-index-url https://download.pytorch.org/whl/cu128

# Pinning a compatible set of torch-related libraries is crucial to avoid dependency conflicts.
# Updated to use PyTorch 2.5.1 for compatibility with our Colab-compatible chatterbox fork
torch==2.9.0+cu128
torchvision==0.24.0+cu128
torchaudio==2.9.0+cu128

# --- Core Application Dependencies ---

# Chatterbox TTS engine - Install from chatterbox-v2 fork
chatterbox-tts @ file:./src/chatterbox
# Core Web Framework
#fastapi
#uvicorn[standard]

# Other ML & Audio Libraries
numpy>=1.24.0,<1.26.0  # Match chatterbox-v2 requirements
onnxruntime==1.16.3
soundfile  # Requires libsndfile system library
librosa
safetensors
descript-audio-codec

# Configuration & Utilities
PyYAML
python-multipart
requests
Jinja2
watchdog
aiofiles
unidecode
inflect
tqdm
hf_transfer                     # Speed up file transfers

# Audio Post-processing
pydub
audiotsm
praat-parselmouth
soundfile

russtress==0.1.4
ruaccent
chardet
